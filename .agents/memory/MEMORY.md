# Memory Index

- [Git push auth](git-push-auth.md) — plain `git push` in shell fails auth; always push via the git-remote skill's `gitPush` callback.
- [iOS touch-scroll freeze](ios-touch-scroll-freeze.md) — never `overflow-x: hidden` on body/page wrappers; use `clip` or touch scrolling freezes on iOS.
- [Deal integer columns](deal-integer-columns.md) — pro forma floats must be rounded before hitting integer deal/ledger columns, or purchases deduct cash then fail.
